#include<stdio.h>
int main ()
{
int n,r,c,arm=0;
printf("Enter the no. of elements ");
scanf("%d",&n);
n=c;
while(n>0)
{
r=n%10;
arm=arm+(r*r*r);
n=n/10;
}
if( c==arm)
{
printf("%d is a arm strong number ",arm);
}
else 
{
printf(" %d is not a arm strong number",arm);
}
return 0;
}
